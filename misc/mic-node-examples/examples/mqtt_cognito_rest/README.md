## Example: MQTT Client using Cognito tokens (updated)
Creates a MQTT client that is authenticated with Cognito. May listen for all Things that the authenticated Cognito user has access to.

## How to run
```
npm install
node index.js
```
